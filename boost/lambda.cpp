#include <algorithm>
#include <boost/lambda/lambda.hpp>
#include <iostream>
#include <iterator>

int main() {
  std::cout << "Hello world" << std::endl;
  for (int i = 0; i < 10; ++i) {
    std::cout << i << std::endl;
  }
}
